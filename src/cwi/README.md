# Python package

The `cwi` package connects grounded language workflows to a three-robot warehouse simulator. Each module documents its implemented responsibility. The installed commands are `cwi-demo`, `cwi-evaluate`, and `cwi-evaluate-llm`. The API factory is `cwi.api.app:app_factory`.
